# T10 / T12 / T15 verification

Date: 17 September 2026

## Scope

Catalogue sync (T10), order submission and admin processing (T12) and the
transactional outbox runner (T15), plus the review repairs listed below
(codes G12–G30; G15, G17 and G20 were handled in earlier changes and required no
further work here, and this evidence document is itself G18) and the schema
change the review required. The catalogue, order and outbox paths are
implemented and green on a frozen tree; this document records the review's
correctness repairs and the verification evidence obtained for them.
Production publishing, dispatch and activation remain disabled behind
unresolved inputs (see "TEST-only policies in force" below).

## Correctness repairs

- G12 — added `listDrainableEvents` and `CatalogueService.drainEvents`: one
  bounded pass over the workspace's pending events plus failed events still
  below the attempt bound, in deterministic `createdAt, id` order, with each
  event's failure recorded and skipped so one poison delivery cannot block the
  queue. T10B schedules it with `reconcile` under the existing `reconcile:run`
  scope.
- G13 — provider adapters are an explicit, non-production opt-in
  (`OUTBOX_ADAPTERS=fake`). A production or unset deployment registers handlers
  that fail permanently with `provider_not_configured`, so a claimed job is
  recorded as failed in admin rather than completing under a fake provider id.
- G14 — request-error logging is PII-safe (`logRequestError`), and the orders
  route returns `503` only for connection-level failures and `500` otherwise,
  instead of misreporting an application error as a provider outage.
- G16 — a completed-idempotency-key retry is bounded by
  `ORDER_RETRY_WINDOW_MS` (24 hours) measured from `completedAt`; outside the
  window the caller receives `draft_expired` rather than a replayed order.
- G19 — recorded the catalogue/order/outbox concurrency and identity decisions
  in `docs/ADR/0003-catalogue-orders-outbox.md`.
- G21 — a correctly signed but malformed webhook body (invalid JSON or an
  invalid payload shape) now returns `400 invalid_request` instead of falling
  through to a generic failure.
- G22 — webhook registry entries require `actorId`; the verified machine actor
  is persisted as `catalogue_sync_events.actor_id` (migration
  `0014_concerned_karen_page.sql`) and reused by every later inbox and sync
  write, so drain attribution is stable and never the event id or a random UUID.
- G23 — each handler runs under a per-handler abort timeout, recorded as
  `uncertain` with `handler_timeout` because the provider may already have
  accepted the work; a run deadline stops the runner claiming new batches while
  still handling and recording jobs already claimed under a lease.
- G24 — the `ORDER_OPERATIONAL_TRANSITIONS` switch is TEST-only and is ignored
  when `NODE_ENV=production`, so it can never authorize a real dispatch or
  activation.
- G25 — an operator requeue of a definitively failed job preserves the previous
  attempt's provider evidence (`provider_id`, `outcome`) and resets only the
  attempt and lease state.
- G26 — `reconcile` reads one `listPublished()` snapshot for the whole pass
  (no per-product N+1 listing), and its actor is required and registry-backed:
  the scheduler entry is resolved through
  `schedulerForWorkspace(workspaceId, 'reconcile:run')`, and the manual command
  fails closed with `reconcile_actor_unavailable` when no single non-revoked
  entry matches.
- G27 — added Sanity catalogue adapter unit tests and registered the adapters
  project with its own `vitest.config.ts`.
- G28 — site-1 catalogue revalidation invalidates a `plan:<productKey>` cache
  tag in addition to the `/plans` index.
- G29 — the runbook's command target `backend` was verified correct as written;
  no change required.
- G30 — the staging cron check cannot run without a dedicated staging Vercel
  project. Recorded as a pending external check below, not a pass.

## Verification

- `pnpm typecheck && pnpm lint && pnpm format && pnpm test` (no DB
  environment): typecheck passed for all workspace projects; ESLint clean;
  Prettier reported "All matched files use Prettier code style!"; Vitest
  reported `Test Files 39 passed | 12 skipped (51)` and
  `Tests 291 passed | 87 skipped (378)`.
- Full database suite on a freshly recreated disposable Postgres
  (`TEST_MIGRATION_DATABASE_URL=postgresql://postgres:postgres@localhost:5432/canadian_plans_test`,
  `DB_TEST_ALLOW_DESTRUCTIVE=1`, runtime
  `DATABASE_URL=postgresql://app_runtime:local_ci_runtime_password@...`,
  `DATABASE_SSL_MODE=disable`, `MIGRATION_DATABASE_URL` and
  `MIGRATION_DATABASE_SSL_MODE=disable`, then `pnpm test`):
  `Test Files 51 passed (51)`, `Tests 378 passed (378)`, 0 failed, 0 skipped.
- Migration `0014_concerned_karen_page.sql` was independently proven
  release-safe on a populated table (nullable add, backfill `actor_id = id`,
  then `SET NOT NULL`; 1 row, 0 nulls afterwards) and applies clean on an empty
  database.
- `pnpm --filter @canadian-plans/adapters run test`: 5 passed, with the
  adapters project now registered through its new `vitest.config.ts`.

## TEST-only policies in force

- `QUOTE_WITHDRAWAL_POLICY` — OPEN_INPUTS #14; unset becomes `unresolved` and
  disables priced checkout.
- `ORDER_OPERATIONAL_TRANSITIONS` — OPEN_INPUTS #15; ignored when
  `NODE_ENV=production`.
- `OUTBOX_ADAPTERS=fake` — non-production only; production has no implicit
  provider.
- `TEST_MIGRATION_DATABASE_URL` with `DB_TEST_ALLOW_DESTRUCTIVE=1` — disposable
  database only.

## Deferrals and pending external check

- Vercel Cron deployment fix (17 September 2026): the minute-level `crons`
  entry added to `apps/backend/vercel.json` with T15 made every backend
  deployment fail at deploy time on the Hobby plan ("Hobby accounts are
  limited to daily cron jobs" — backend deployments failed from `51416c7`
  onward while admin/site-1 stayed green). The entry is removed until the
  dedicated staging Vercel project on Pro exists; T10B re-adds it together
  with the five-minute catalogue sync cron. The authenticated
  `POST /api/internal/jobs/run` is the interim invocation path
  (docs/RUNBOOKS/outbox-jobs.md). **Superseded by the Railway backend
  migration** ([ADR 0004](../ADR/0004-backend-on-railway.md)): the schedule
  now runs in-process on the Railway service rather than as Vercel Cron.
- T10B schedules `drainEvents` together with `reconcile` under the
  `reconcile:run` scope; the route and runner are implemented and locally
  verified (see "T10B — scheduled catalogue sync" below). The hosted staging cron
  check is still pending the external deployment described there.
- T16 may exercise dispatch and activation only synthetically until
  OPEN_INPUTS #15 is resolved.
- The site-1 `plan:<productKey>` tag has no consumer yet because
  `apps/site-1/src/app/(storefront)/plans/[slug]/page.tsx` is still a
  placeholder; the catalogue-UI task must attach the tag.
- Manual `reconcile` needs a non-revoked scheduler entry holding
  `reconcile:run`.
- G30 CLOSED (18 September 2026): the scheduled-execution check now runs on the
  Railway backend deployment (ADR 0004), superseding the Vercel-staging-project
  path this bullet originally described. See "T10B — scheduled catalogue sync"
  below for the observed evidence.

## T10B — scheduled catalogue sync

Date: 17 September 2026

The already-tested T10 handlers are now registered with T15's authenticated cron
surface. `createCatalogueSyncRouter` exposes `GET`/`POST
/api/internal/catalogue/sync` (mounted at `/api/internal/catalogue` in
`app.ts`), mirroring `/api/internal/jobs/run`: it resolves the scheduler identity
through the server-only machine registry (`resolveScheduler`), requires the
existing `reconcile:run` scope selected by `CATALOGUE_SYNC_SELECTOR`, rejects
preview deployments, and derives its workspace set **only** from the verified
identity — never from a database tenant scan. `CatalogueSyncRunner` then runs,
for each authorized workspace in registry order, one bounded
`CatalogueService.drainEvents` pass followed by `CatalogueService.reconcile`
using the registry entry's `actorId`. A workspace whose drain or reconcile fails
records a bounded error code and the pass continues; a 120-second run deadline
stops starting new workspaces while work already in flight is recorded. No new
machine scope and no schema migration.

### What was exercised (disposable Postgres, freshly recreated)

`apps/backend/tests/catalogue-scheduled-pass.integration.test.ts` drives the real
runner against a migrated disposable database and covers the three cases the
task names, through the scheduled path rather than the manual handler:

- **Duplicate delivery** — the same delivery id accepted twice yields one inbox
  row (`duplicate: true` on replay); the scheduled pass reports `listed: 1`,
  `processed: 1` and writes exactly one offer version.
- **Out-of-order events** — an r2 delivery and an r1 delivery are both pending,
  with `created_at` forced so the stale r1 delivery drains _after_ the newer r2
  delivery; the pass converges on revision r2 with a single offer version, and
  both inbox rows complete against r2.
- **Missing webhook** — a product published in the CMS with **no** inbox event is
  created by the reconciliation half alone (`listed: 0`, `reconciled: 1`), at the
  current revision and unrevoked.

Route-level unit tests (`catalogue-sync-route.test.ts`) prove the unauthorized
paths: a wrong secret returns 401, an `outbox:run`-only scheduler returns 403, a
preview deployment returns 403, and only the registry-authorized workspace set
reaches the runner with the persisted actor. `catalogue-runner.test.ts` proves
the deadline stops new workspaces without abandoning the one in flight, that a
failing stage is recorded while the remaining workspaces continue, and that
per-event drain failures surface in the summary.

### Verification

- `pnpm typecheck` ✅, `pnpm lint` ✅, `pnpm format` ✅.
- No-DB run: `Test Files 41 passed | 13 skipped (54)`,
  `Tests 298 passed | 90 skipped (388)`.
- Full DB run on a freshly recreated disposable Postgres:
  `Test Files 54 passed (54)`, `Tests 388 passed (388)`, 0 failed.
  (Baseline before T10B: 51 files / 378 tests.)

### Staging cron check — PASSED, G30 closed (18 September 2026)

The backend is provisioned on Railway (project `canadian-plans-backend`,
service `backend`, production environment, region US West) with
`ENABLE_SCHEDULER=1`, a live `DATABASE_URL` against the production Supabase
project, and `MACHINE_REGISTRY_JSON` holding one non-revoked scheduler entry
(`selector: outbox-runner`) with `outbox:run` and `reconcile:run`, an explicit
`actorId`, and both production workspace UUIDs. `JOB_RUNNER_SELECTOR` and
`CATALOGUE_SYNC_SELECTOR` both select that entry; `CRON_SECRET` matches its
`secret`.

**Manual invocation** — `POST /api/internal/jobs/run` and
`POST /api/internal/catalogue/sync`, both with the real `CRON_SECRET` and
`X-Scheduler-Selector: outbox-runner`, each returned `200` and processed both
registry-authorized workspaces:

```
POST /api/internal/jobs/run
→ 200 {"workspaces":2,"claimed":0,"completed":0,"retried":0,"failed":0,
       "uncertain":0,"leaseLost":0,"requestId":"2b2ca983-450e-4c7a-b24d-46bfc3594c33"}

POST /api/internal/catalogue/sync
→ 200 {"workspaces":2,"listed":0,"processed":0,"drainFailed":0,"reconciled":0,
       "failures":[
         {"workspaceId":"10000000-0000-4000-8000-000000000101","stage":"reconcile","errorCode":"provider_unavailable"},
         {"workspaceId":"10000000-0000-4000-8000-000000000102","stage":"reconcile","errorCode":"provider_unavailable"}
       ],"requestId":"8b00b127-f390-46e5-a234-4a2a49626be0"}
```

`provider_unavailable` on both workspaces is expected and correct, not a gap in
this check: no `webhooks` entry exists yet in `MACHINE_REGISTRY_JSON` mapping
either workspace to a Sanity project, because that entry's required
`sanity.revalidateUrl` must point at a publicly reachable site-1 deployment
(schema in apps/backend/src/machines/registry.ts), and site-1 has no public
URL yet (still behind Vercel Deployment Protection). What this check proves —
the scheduler resolving its identity from the registry, connecting to the real
database, and running the bounded per-workspace pass with graceful per-stage
failure recording — is exactly what T10B specifies; wiring the Sanity webhook
entry itself is tracked separately, gated on site-1 going public.

**The in-process scheduler, unprompted** — no request from any operator or
tool triggered these; both fired from the service's own timers after
`ENABLE_SCHEDULER=1` was set and the service redeployed at
`2026-09-17T21:51:15Z` (`backend listening on http://localhost:8080`):

```
app.catalogue_sync_state after boot:
  workspace …101  last_attempt_at 2026-09-17 21:51:29.863+00  last_error_code provider_unavailable
  workspace …102  last_attempt_at 2026-09-17 21:51:30.169+00  last_error_code provider_unavailable
```

14–15 seconds after boot — matching the scheduler's 10-second startup delay
plus up to 5 seconds of jitter exactly (apps/backend/src/scheduler.ts).
Re-queried 75 seconds later: both `last_attempt_at` values were **unchanged**,
confirming the 300-second catalogue-sync interval is honoured and the scheduler
is not firing on the outbox's 60-second cadence by mistake. Across that whole
window — spanning at least one, likely two, outbox ticks — the deployment logs
carried **zero** `scheduler_task_failed` or `scheduler_*` configuration-error
lines: the outbox ticked on its own schedule and completed cleanly with nothing
to claim.

**G30 is closed.** The five-minute (now in-process) schedule has run on a
deployed, non-preview environment against the real production database, been
observed ticking unprompted on its own timer at the correct cadence, and its
manual-invocation counterpart independently confirmed the same code path with a
recorded request and response. The remaining Sanity-webhook wiring is a
separate, tracked, non-blocking follow-up once site-1 has a public URL.

## Documentation changed

- `docs/ADR/0003-catalogue-orders-outbox.md` (new)
- `docs/API.md`
- `docs/ENV.md`
- `docs/RUNBOOKS/catalogue-sync.md`
- `docs/RUNBOOKS/orders.md`
- `docs/RUNBOOKS/outbox-jobs.md`
- `docs/SCHEMA_HISTORY.md`
- `docs/EVIDENCE/T10-T12-T15.md` (new)
- `jobs/README.md`
