# A1 isolation and preview-boundary evidence

Date: 2026-09-14

## Scope and gate interpretation

This evidence covers REQUIREMENTS §16 Phase A gates 2 and 11, REQ 48, and
IMPLEMENTATION_PLAN §5 **Database enforcement** for the routes implemented
through T5/T6. Real order, transition, attachment, and file-route isolation
remains assigned to T16/T23 when those routes exist; this evidence does not
claim those later checks.

The external Supabase Auth HTTP service is replaced by a local fixture that
accepts only issued signed tokens and enforces their expiry. The production
Supabase SDK and session verifier, HTTP routers, authorization policy, database
stores, restricted runtime role, RLS policies, credential bootstrap, and machine
registry are real implementations. This is not hosted Auth/MFA recovery proof.

## Isolation suite

`apps/backend/tests/isolation.spec.ts` uses two synthetic workspaces and tests:

1. Altered staff workspace path IDs are denied, and foreign membership and
   service-credential fixture IDs return not found without changing the foreign
   records.
2. A membership removed behind an otherwise valid live session is denied on
   the next request.
3. Expired/forged staff bearer credentials and forged/revoked website service
   credentials are denied.
4. Body/Host/Origin workspace claims cannot override the website credential's
   workspace; a forged role header cannot escalate a viewer on the existing
   protected service-credential action.
5. Missing database context sees no tenant rows, and sequential tenant
   transactions on the same one-connection pool do not reuse `SET LOCAL`
   workspace context.
6. The server-only machine registry rejects provider-account and expected-
   workspace confusion even with a signature valid for another mapping.
7. Re-invitation restores a revoked staff membership using only fresh roles,
   removes stale permission overrides, serializes repeated acceptance, and
   consumes an active member's invitation without silently escalating roles.
8. Bursts of 20, 40 and 100 simultaneous limiter calls allow exactly 10 per
   bucket; independent buckets remain available on the one-connection pool.
   This measures local queue completion, not hosted Supavisor saturation or a
   production latency SLO. Per-burst elapsed milliseconds are emitted by the test.

## CI enforcement

- `pnpm run check:preview-isolation` validates
  `docs/DEPLOYMENT_INVENTORY.json`. A provisioned preview credential must map to
  a same-kind preview/non-production resource identified by a SHA-256
  fingerprint of its provider-issued immutable ID. Name-only resources,
  missing bindings, and fingerprints reused from production fail. The current
  inventory accurately records all three preview deployments as not provisioned
  with no credentials; it must be updated in the provisioning change.
  Empty inventories, missing required apps/catalog entries, and provisioned
  deployments without fake customer email and an independent Auth SMTP sink
  also fail. These are committed-inventory checks, not live secret inspection.
- `pnpm run check:frontend-db-boundary` runs the repository ESLint boundary over
  `apps/admin` and `apps/site-1`. Existing tooling fault cases prove static,
  subpath, dynamic, relative, CommonJS, and type-only imports of
  `@canadian-plans/db` fail.
- Both checks are named mandatory steps in `.github/workflows/ci.yml`, before
  compilation and tests. CI first migrates disposable PostgreSQL and exports
  both database-test opt-ins. The isolation suite explicitly throws when CI is
  set but either opt-in is absent. Generated OpenAPI drift also fails CI.

## Earlier isolation run (before finalization fixes)

| Check                                                                                        | Result                                                                                                                             |
| -------------------------------------------------------------------------------------------- | ---------------------------------------------------------------------------------------------------------------------------------- |
| Disposable PostgreSQL 15 migration + `pnpm --filter backend test -- tests/isolation.spec.ts` | **PASS** — 1 file, 6 tests                                                                                                         |
| `pnpm run check:preview-isolation`                                                           | **PASS** — 0/3 preview deployments provisioned, 0 credential bindings; current state is explicit                                   |
| `pnpm run check:frontend-db-boundary`                                                        | **PASS**                                                                                                                           |
| `pnpm run test:tooling`                                                                      | **PASS** — 61 tests, including 6 preview-inventory fault cases and frontend boundary fault cases                                   |
| `pnpm run typecheck`                                                                         | **PASS** — all 9 workspace projects                                                                                                |
| `pnpm run lint`                                                                              | **PASS**                                                                                                                           |
| `pnpm run test` without local DB variables                                                   | **PASS** — 129 passed, 24 DB-gated skipped; the 6 new DB-gated cases were then run against disposable PostgreSQL as recorded above |
| `pnpm run build`                                                                             | **PASS** — contracts generation, backend, admin, and site-1                                                                        |
| Targeted Prettier check for changed files and `git diff --check`                             | **PASS**                                                                                                                           |

The disposable database and embedded PostgreSQL package used for that focused
run were outside the repository and moved to the Recycle Bin after the run. No
production or connected development resource was used, and no credential value
is present in this evidence or the deployment inventory.

## Changed documentation

- `docs/DEPLOYMENT_INVENTORY.json`
- `docs/ENV.md`
- `docs/EVIDENCE/A1.md`

## Foundation finalization verification — 2026-09-14

The following results supersede the earlier run above for the updated code:

| Check                                                            | Result                                                                                                                                 |
| ---------------------------------------------------------------- | -------------------------------------------------------------------------------------------------------------------------------------- |
| Empty disposable PostgreSQL 15 migration, then full Vitest suite | PASS: 21 files, 160 tests, zero skipped; includes all 8 isolation cases                                                                |
| Tooling fault-injection suite                                    | PASS: 63 tests, including 8 preview inventory cases                                                                                    |
| Typecheck                                                        | PASS: all 9 workspace projects                                                                                                         |
| ESLint and frontend DB boundary                                  | PASS                                                                                                                                   |
| Preview inventory check                                          | PASS: 0/3 deployments provisioned, zero credential bindings; not deployed-environment sign-off                                         |
| Build                                                            | PASS: backend, admin, site-1; OpenAPI 3.1 generated with 22 paths                                                                      |
| Browser checks                                                   | PASS: 18 admin shell and 22 site-1 tests, desktop/mobile; these do not exercise live authenticated staff journeys                      |
| Site-1 boundary/bundle scan                                      | PASS: deliberate browser import of server config rejected; synthetic credential absent from 45 browser assets and 40 rendered payloads |

The final focused isolation rerun passed all 8 cases after strengthening the
forged token fixture. Local 20/40/100-call bursts completed in 13/24/60 ms,
respectively (one pooled connection, cap 10; not a hosted performance claim).
A deliberate run with CI enabled and missing DB opt-ins failed as intended.
Regenerating OpenAPI produced no diff from the staged document.

The CORS preflight and logout/account-switch regressions were reproduced before
fixing and pass afterward. Migration `0005_reinvitation.sql` was applied only
to disposable local PostgreSQL. The default website write route now denies
unconfigured edge/bot admission before database work; no arbitrary forwarding
header selects the IP limiter bucket. Real public writes remain disabled until
a verified deployment admission implementation is wired in.

Tracked foundation files received mechanical Prettier normalization to fix
working-copy formatting failures (including CRLF differences). Untracked owner skill files are deliberately
excluded from this commit; their pre-existing formatting still makes a blanket
local `pnpm format` fail. The committed-file formatting check is the relevant
clean-checkout check. The OpenAPI generator now emits formatted JSON itself.

### First-to-last foundation status

| Task                            | Status                                                                                                                                                                 |
| ------------------------------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| T0 accounts/resources           | **OPEN**: only connected Supabase project is `us-west-2`, not required `ca-central-1`; Canadian Plans Vercel team has no projects                                      |
| T1 monorepo/CI/boundaries       | Implemented and locally verified; no hosted release was performed                                                                                                      |
| T2 admin shell                  | Implemented; shell/browser checks pass                                                                                                                                 |
| T3 storefront shell             | Implemented; browser and credential-bundle checks pass                                                                                                                 |
| T4 schema/RLS                   | Implemented; migrations, restricted-role and pooling checks pass locally                                                                                               |
| T4R recovery feasibility        | **OPEN**: nine synthetic semantic tests are not supported provider export/import, password/TOTP recovery, real encrypted object recovery or restricted B2-writer proof |
| T5 staff access                 | Implementation and regressions pass; real signup/invitation, MFA, account-switch and removal browser journeys await isolated Auth/SMTP setup                           |
| T6 credentials/machine registry | Local authorization/isolation checks pass; verified edge/bot admission and hosted abuse/pool measurement remain open                                                   |
| T7 shared contracts/client      | Implemented, consumed by both frontends, generated and tested; schemas do not imply future handlers exist                                                              |
| T8 isolation/evidence           | Local suite and named CI checks pass; deployed preview credentials cannot be attested until resources exist                                                            |

**Foundation sign-off is not granted. Do not start the order slice yet.**
The local fixes do not waive T0/T4R prerequisites. OPEN_INPUTS #22 needs the
owner's staging-organization/billing choice, followed by cost verification and
Canadian staging/recovery provisioning. Required follow-through: isolated fake
Auth SMTP, real provider recovery spike with separately controlled R2/Sanity/B2
access, authenticated staff browser journeys, verified edge/bot integration,
and an observed deployment credential inventory. No production resources were
changed, no migration was applied to hosted Supabase, and no email was sent.
