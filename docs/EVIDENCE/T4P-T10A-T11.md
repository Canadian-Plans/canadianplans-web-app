# T4P / T10A / T11 verification

Date: 2026-09-16

## Scope

Partner schema, catalogue schema prerequisites, lead capture and draft grants.
T10 catalogue sync, T12 orders and T15 outbox work were not started.

## Correctness repairs

- Migration `0008_first_garia.sql` drops the previous
  `resolve_website_credential(text)` signature before recreating it with the
  credential ID result column.
- Migration `0009_brainy_revanche.sql` adds the workspace-scoped lead-to-partner
  relationship and prevents product deletion from cascading into immutable
  offer versions.
- Approved partner codes persist `leads.partner_id`; unknown, suspended and
  foreign-workspace codes remain bounded attribution with no partner link.
- Lead creation and updates write audit events in the same tenant transaction.
  Audit values contain state and changed-field names, not customer contact or
  form values.
- Approved referrer URLs are reduced to an approved hostname. Landing paths are
  limited to the actual site-1 routes, including `/plans/[slug]`. Overlong,
  token-like and contact-like attribution values are dropped.
- The T11 task route text now matches the established website API namespace.

## Verification

- Clean PostgreSQL 15.14 migration: passed through migration 0009.
- Full Vitest run with the disposable database enabled: 28 files passed,
  229 tests passed, 0 skipped.
- TypeScript: all 9 executable workspace projects passed.
- ESLint: passed.
- Frontend database boundary: passed.
- Tooling tests: 65 passed.
- Production builds: backend, admin and site-1 passed.
- Browser/security verification: admin 18 tests passed; site-1 credential scan
  passed; site-1 desktop/mobile journeys 22 tests passed in each configured
  security run.
- OpenAPI generation: two consecutive generations produced SHA-256
  `458A738A7477665B99B332E36B8F34F405C80B22E7168BF232A758871DD1CC90`.
- `git diff --check`: passed.
- Prettier: all files changed by the review repair passed targeted checks.

The repository-wide Prettier check on this Windows checkout also scans unrelated
untracked `.agents/skills` files and reports existing checkout line-ending
differences. Those files were outside this task and were not rewritten.

## Documentation changed

- `docs/BUILD_TASKS.md`
- `docs/SCHEMA_HISTORY.md`
- `docs/api/openapi.json`
- `docs/EVIDENCE/T4P-T10A-T11.md`
