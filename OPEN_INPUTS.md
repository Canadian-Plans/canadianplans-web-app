# Open Inputs

Unanswered business questions. Add to this file rather than guessing past it.

## UI / Admin

- **Real workspace list for WorkspaceSwitcher.** The admin shell (T2) uses a
  hardcoded placeholder list (`apps/admin/src/lib/workspaces.ts`) with
  site-1/site-2/site-3. Needs a backend "my memberships" endpoint per
  PLATFORM_CONTEXT.md §4b before this can be real. Not a business-rule
  question — flagging so it isn't mistaken for finished wiring.
